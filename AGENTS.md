# Repository Instructions

- After completing requested changes, commit them and push/publish them to GitHub unless the user explicitly asks not to.
- When an APK is produced, bump the Android build version code and publish the APK in a GitHub pre-release as a release asset, unless the user explicitly asks not to.
